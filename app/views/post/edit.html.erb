<%= form_errors(@post) %>

<%= form_for @post, url: {action: 'update'} do |form| %>
  <%= form.label :title %>
  <%= form.text_field :title %>
  <br />
  <%= form.label :body %>
  <%= form.text_area :body %>
  <br />
  <%= form.submit 'Update' %>
<% end %>
